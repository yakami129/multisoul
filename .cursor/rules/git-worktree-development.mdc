---
description: Require git worktree workflow; forbid day-to-day code edits on main checkout
alwaysApply: true
---

# Git worktree 开发（强制）

在本仓库进行**产品代码变更**时：

1. **必须使用 `git worktree`**（或等效隔离检出目录）承载日常编辑：在**功能分支**（如 `feat/*`、`fix/*`）上开发。Worktree 路径可放在仓库下的 `.worktrees/`（已在 `.gitignore`）或你自选的并列目录。创建与清理流程优先遵循 Superpowers skill **`using-git-worktrees`**。
2. **禁止**在**当前分支为 `main`**（或本仓库默认发布分支）的检出目录里直接修改 `mobile/**`、`cli/src/**` 等源码并 `git commit`。允许的仅是 **`git pull` / `git merge` / `git fetch` / 分支切换**等不产生未合并特性 diff 的维护操作。
3. 开始改代码前应确认：`git branch --show-current` 不是 `main`，且当前目录是为此任务准备的 worktree（或等价隔离工作区）。若发现正站在 `main` 检出上改业务代码，应先 **`git worktree add`**（或新建分支 + 切到独立目录）再编辑。

合入方式仍须遵循 **`AGENTS.md` / `CLAUDE.md`**（PR、禁止直推 `main`）及 [`docs/product-specs/SPEC-pr-workflow.md`](docs/product-specs/SPEC-pr-workflow.md)。
