# msctl

Install the MultiSoul CLI with npm:

```bash
npm install -g @yakami129/msctl
msctl --version
```

This package bundles platform binaries, so installing it does not require a GitHub token.

Supported platforms:

- macOS arm64
- Linux x64
- Windows x64
