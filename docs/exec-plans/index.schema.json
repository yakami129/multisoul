{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://github.com/yakami0129/multisoul/docs/exec-plans/index.schema.json",
  "title": "exec-plans manifest",
  "type": "object",
  "required": ["version", "documents"],
  "properties": {
    "$schema": {
      "type": "string",
      "description": "Optional JSON Schema pointer for editor validation."
    },
    "version": {
      "type": "integer",
      "const": 1
    },
    "documents": {
      "type": "array",
      "description": "One entry per plan markdown in this directory (excluding README.md). Prefer reverse lexicographic order by file (newest first).",
      "items": {
        "type": "object",
        "required": ["file", "title"],
        "additionalProperties": false,
        "properties": {
          "file": {
            "type": "string",
            "pattern": "^\\d{4}-\\d{2}-\\d{2}-[a-z0-9-]+\\.md$"
          },
          "title": {
            "type": "string",
            "minLength": 1
          },
          "lastCompletedCommit": {
            "type": "string",
            "description": "Full 40-char lowercase git object name (SHA-1) of the single commit that completed this plan after all tasks passed verification; set by humans/agents after that commit. Omit until set.",
            "pattern": "^[0-9a-f]{40}$"
          }
        }
      }
    }
  },
  "additionalProperties": false
}
